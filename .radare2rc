# Display stack and register values on top of disasembly view (visual mode)
e cmd.stack = true

# Solarized theme
eco solarized
# 256 color support
e scr.color=2


# Enabled pager for scrolling
e scr.pager=less -R

# Use UTF-8 to show arrows
e scr.utf8 = true
